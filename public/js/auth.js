"use strict"
+function(f){
    if("function" === typeof f)
        f();
}(function(){

    // Set the 100% height of Page
    // var fullHeight = $(window).height();
    // $("#auth-banner").height(fullHeight-100);
    // $("#auth-box").height(fullHeight);

})
